## The sanity groq queries need to put under the lib forder of the sanity which added along with the installment of the sanity packages.

## The main reason that i have added to the lib outside the sanity for the better folder structure and in future if need to made any changes to the query or the schema where the need to shift the folder first to the sanity lib folder then run the pnpm run typegen command.
